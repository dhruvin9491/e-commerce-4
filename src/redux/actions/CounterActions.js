import { COUNTER_ACTION } from "../../constants/ActionConstant";

export const increment = { type: COUNTER_ACTION.INCREMENT }
export const decrement = { type: COUNTER_ACTION.DECREMENT }
export const reset = { type: COUNTER_ACTION.RESET }